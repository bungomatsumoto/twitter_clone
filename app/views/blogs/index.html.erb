<h1>投稿一覧</h1>

<table>
  <tr>
    <th>内容</th>
  </tr>

<% @blogs.each do |blog| %>
  <tr>
    <td><%= blog.content %></td>
    <td><%= link_to "詳細を確認する", blog_path(blog.id) %></td>
  </tr>
<% end %>
</table>

<%= link_to '新しく投稿する', new_blog_path %>
